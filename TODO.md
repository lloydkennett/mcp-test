make some tools utils package?

generate mcp.tooresponce from structs
validate notempty input vals

change registry pkg to tools and add utils for converting any type to mcp.Result

combine related tools like mergerequets

test mounting docker.sock to devcontainer, document the install process